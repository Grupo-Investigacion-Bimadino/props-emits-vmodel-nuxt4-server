{
  "name": "music-app-server",
  "version": "1.0.0",
  "description": "JSON Server backend para Music App",
  "type": "module",
  "scripts": {
    "dev": "json-server --watch db.json --port 4000",
    "start": "json-server db.json --port 4000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
